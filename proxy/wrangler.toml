name = "livekit-mcp-proxy"
main = "worker.js"
compatibility_date = "2024-09-02"

routes = [
  { pattern = "livekit-mcp.webfuse.it", custom_domain = true }
]

[vars]
# Set via `wrangler secret put`:
# ORIGIN_URL - token server tunnel URL
